# App-Project-

This Streamlit app now includes:

- user signup with password hashing
- login for returning users
- saved daily check-ins and cognitive challenge progress
- a proper multipage setup using `main.py`, `login.py`, and `pages/`
- a Help page that only appears after login

Run it with:

```bash
streamlit run main.py
```
